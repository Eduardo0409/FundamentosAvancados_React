
const ExecutionFunction = ({ myFunction }) => {
    return (
        <div>
            <button onClick={myFunction}>Clique aqui para executar a funcao!</button>
        </div>
    )
}

export default ExecutionFunction